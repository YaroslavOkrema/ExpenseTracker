export enum TransactionType {
  INCOME = 'income',
  EXPENSE = 'expense',
}

export enum Theme {
  DARK = 'dark',
  LIGHT = 'light',
  SYSTEM = 'system',
}

export enum Language {
  EN = 'en',
  UA = 'uk',
}

export enum LocalStorageKeys {
  LOCALE = 'locale',
  THEME = 'theme',
}
